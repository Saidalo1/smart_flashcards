# Core dependencies with pinned versions for reproducibility
PyQt6==6.10.1
rapidfuzz==3.14.5  # fast, typo-tolerant string grading (first pass)
# sentence-transformers + torch power the semantic (synonym) grading fallback.
# They are optional at runtime: with semantic_grading=False the app never imports
# them, so a strict/lightweight build can drop torch entirely.
sentence-transformers==5.1.2
torch==2.8.0
pyinstaller==6.17.0
pynput==1.7.7
evdev==1.9.2  # kernel-level global hotkeys on Linux/Wayland (needs 'input' group)

# Main transitive dependencies (pinned for stability)
transformers==4.57.3
huggingface-hub==0.36.0
tokenizers==0.22.1
numpy==2.0.2
scikit-learn==1.6.1
scipy==1.13.1
